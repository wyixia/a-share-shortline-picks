---
name: a-share-shortline-picks
description: A股短线选股 · 生产技能包（自带数据准备与已训练权重）。自动化任务或人工会话只需触发它：给一个交易日，它自己拉全市场数据、出候选名单、生成 HTML 报告。也可选做权重重训。数据目录默认本包旁的 picks_data/（环境变量 PICKS_DATA_DIR 可改）。
agent_created: true
---

# A股短线选股 · 生产技能包

**本技能自带数据准备与已训练权重**：给它一个交易日，它自己拉数据 → 出名单 → 生成 HTML 报告。
自动化定时任务、人工会话，都只是**触发**它，不需要关心内部实现，也不需要预先准备任何数据。

```
python scripts/数据准备.py 2026-09-21      # ① 数据准备（首次约 10-15 分钟，之后每天约 5-8 分钟）
python scripts/出名单.py 2026-09-21        # ② 出名单 → HTML 报告 + CSV
```

数据目录：环境变量 `PICKS_DATA_DIR`，未设则默认本包旁的 `picks_data/`（自动创建）。交付一律 HTML/Markdown 表格。

---

## 任务 0：数据准备（每次出名单前先跑）

```
python scripts/数据准备.py <交易日> [--days 45]
```

自动做三件事（幂等，已有的跳过）：
| 步骤 | 产物 | 说明 |
|---|---|---|
| ① 全市场快照 | `close_snap_latest.json`、`_all_codes.txt` | 名称/流通股本/换手/成交额（来源=东方财富 clist，一次性列表）。**30 天内有效则跳过** |
| ② 近 N 自然日全市场日 K | `daily/<T>/kline.jsonl` + `history_kline_<T>.jsonl` | 含 `amount`/`exchange`；供因子与历史窗口用 |
| ③ T 日资金流 | `daily/<T>/mf.jsonl` | 腾讯全字段（含 `MainNetFlow`） |

- **首次使用**（数据目录为空）：约 10-15 分钟。
- **之后每天**：只拉当日增量，约 5-8 分钟。
- 时点说明：当天**盘中**跑拉到的是盘中价；**正式名单以 14:45 之后的快照为准**。

### 数据依赖分层（什么必须、什么补数才跑、什么重训才跑）

| 层级 | 内容 | 什么时候需要 | 由谁生成 |
|---|---|---|---|
| **① 出名单·必须** | 快照 `close_snap_latest.json`、历史K线 ≥21 个交易日、T 日K线 | 每个要出名单的交易日（历史K线首次一次） | `数据准备.py <T>` |
| **② 补数·缺了才跑** | 补某日快照/K线/资金流断点；长窗口历史K线 | 换机器、数据缺失时；重训前 | `数据准备.py <T>`（幂等补缺）/ `补数_历史K线.py <起> <止>` |
| **③ 重训·才需要** | 历史资金流全字段 + `训练权重.py` + 验证纪律 | 仅重训权重时 | `补数_资金流.py`（随包 CLI 跑腾讯口径）→ `训练权重.py` |

- 日常只与层级 ①② 打交道；`补数_历史K线.py`、`补数_资金流.py`、`训练权重.py` 平时不用碰。
- 数据目录默认本包旁 `picks_data/`（环境变量 `PICKS_DATA_DIR` 可改）。
- 新环境最短路径：`python scripts/数据准备.py <T>` → `python scripts/出名单.py <T>`。中途报缺数据，按报错回到上表对应层级补齐。

---

## 任务 1：出名单并生成 HTML 报告（核心任务）

**固定参数（不得随意改）**
- 池子 = 全市场剔 ST → 换手 ≥ 1% → 成交额 ≥ 1亿
- 排序 = `weights/统一三开.json`
- 取前 30，**剔封板**（实测 Top30 里平均仅 0.3 只封板、含/剔收益相同；剔了保证名单必然可买）
- 买卖口径 = **T+1 开盘买 → T+2 收盘卖**

```
python scripts/出名单.py <T>
```
- **交付 HTML 报告** `候选_<T>.html`（脚本自动生成，表格已渲染好，自带口径说明）
- `候选_<T>.csv` 是数据件，随报告一并给出

---

## 任务 2：算某期效果（可选）

等 T+1、T+2 两个交易日走完，`收益 = T+2收盘 / T+1开盘 - 1` 逐只计算，等权平均即该期收益。
（参考写法：逐只计算后等权平均）

---

## 任务 3：重新训练权重（可选，需历史数据）

```
python scripts/训练权重.py
```
产物写入本包 `weights/`。**训练完必须过验证（见下），否则不得替换 `weights/统一三开.json`。**

---

## 任务 4：补更早的历史（可选，仅重训长窗口时需要）

`python scripts/补数_历史K线.py`、`python scripts/补数_资金流.py`（回补 2021-09 起的多年数据）

---

## 验证（重训后必须全过，缺一不可）

1. 先筛选 → 在筛后池内重跑权重
2. 双向拟合：IS(2025-04-23~) 与 OOS-B(2021-09~2024-03) 互为样本外
3. 排序层：全池日度秩 IC + 月同向率，10 口径（A=T收买→T+j开/收卖；B=T+1开买→T+j开/收卖）
4. 组合层：TopN 绝对收益，IS / OOS-A / OOS-B 三段全为正
5. 5 折时间交叉（>40/45 为正）+ bootstrap 5% 分位为正
6. 判定线：**IC > 0.01、月同向 ≥ 90%、组合三段为正、双向成立**

---

## 口径规则（执行前提，不得违反）

1. **成交额 = 量 × (1 if 688/689 else 100) × 价**；数据源直给 `amount`/`exchange` 时直接用直给值。
2. 因子一律从 K 线自算，**不使用任何含次日信息的字段**。
3. 资金流口径（腾讯 / 新浪 / 通达信）**不可混用**，选定一套用到底。
4. T 日的因子用 14:45 快照；T+1 开盘价、T+2 收盘价用官方价。
5. 因子/权重在**单票层**拟合；效果以**组合层**（TopN 等权）判断。
6. 同向性 = 排名高低 vs 区间收益的秩相关 + 名次梯度 + 头档−池均。
7. 样本纪律：单日/几十只不下结论；标准是 1000+ 天、双向样本外、月同向。
8. 成本双边 0.1~0.2%。

---

## 执行环境（每次都适用）

- 依赖：Python 3.x + numpy、`node`（跑随包 CLI，路径可用环境变量 `NODE_EXE` 覆盖）、`curl`（拉快照）。
- 一切取数优先读本地文件；确需现拉时用普通 HTTP/CLI，遇到限频 → 用本地已有数据，**不要反复重试**。
- 交付一律 HTML / Markdown 表格，不给 .py / .log / .txt 原始件。

---

## 作业纪律

- 写新脚本前先查本包与数据目录已有脚本，能复用就复用；先小批量试再全量跑。
- 重训产物未过「验证」纪律，不得替换 `weights/统一三开.json`。
