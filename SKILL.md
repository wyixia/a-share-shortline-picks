---
name: a-share-shortline-picks
description: A股短线选股 · 生产技能：出某日候选名单并生成 HTML 报告（持有口径=次日开盘买、第三日收盘卖）。当用户要"出今日/某日候选名单""看某天的票""跑选股"时使用。数据目录默认 F:/aigp/alt_screening。
---

# A股短线选股 · 作业手册

工作目录 `F:/aigp/alt_screening`（环境变量 `PICKS_DATA_DIR` 可改）。交付一律 HTML/Markdown 表格。

---

## 〇、会话分工（先读，避免越界）

| 会话 | 职责 | 产出 |
|---|---|---|
| **生产定时任务**（工作日 14:40，由原平台自动化会话负责；其他环境没有此任务） | 拉快照 → 出生产名单 → 写报告 → 回填绩效 | `daily/<日期>/kline.jsonl`、`mf.jsonl`、`选股报告_*`、`record.jsonl` |
| **本会话（查询/研究）** | **只读**磁盘上已有的生产产物；按需跑**研究线**出新方案名单；**不重跑生产** | `候选_新方案_<日期>.csv`、分析报告 |

→ 本环境 **不要运行 `daily_run.py`**，那是生产定时任务的活。
→ 当天 14:40 之前查询：生产快照还没生成，**如实说明**，不要现拉顶替。

---

## 一、两条线的口径对照（别混）

| 线 | 谁在跑 | 权重 | 买卖口径 | 产物 |
|---|---|---|---|---|
| **生产线** | 自动化会话 | `weights_v3.json` | T 收买 → T+1 开卖（A1open） | `daily/<日期>/选股报告_*` |
| **研究线（新方案）** | 普通会话按需 | `权重_修正_统一三开IS.npy` | **T+1 开买 → T+2 收卖（B2close）** | `候选_新方案_<日期>.csv` |

→ 用户要"某天的名单"：**先走任务 0**（只读已有产物）；明确要"新方案"的名单才走任务 1。
→ 两线口径不同，交付时必须注明用的是哪条线，不要混着说。

---

## 二、环境要求（每次都适用）

- Python 3.x + numpy；命令一律 `python scripts/xxx.py <日期>` 的形式。
- 数据目录：环境变量 `PICKS_DATA_DIR`，默认 `F:/aigp/alt_screening`（须可访问；含 `daily/` 快照与历史 jsonl，约 2.9 GB）。
- 一切取数优先读本地文件；确需现拉时用普通 HTTP/本地 CLI，**不依赖任何平台的 MCP 连接器**。
- 遇到数据源限频 → 改用本地数据，**不要反复重试**。
- 交付一律 HTML / Markdown 表格，不给 .py / .log / .txt 原始件。

---

## 任务 0：查并交付已有产物（只读，先做这个）

1. 查 `daily/<交易日>/选股报告_<交易日>_速览.html`（及 `_详细.html`）→ **生产报告**（口径 = weights_v3 + A1open）
2. 查 `候选_新方案_<交易日>.csv` → **研究线名单**（口径 = 综合A权重 + B2收）
3. 有哪个交付哪个，**注明口径**；两个都有就并列交付、分开展示。
4. 都没有 → 如实说明（14:40 之前的当天查询属正常），可转任务 1。

---

## 任务 1：出某日名单并生成 HTML 报告（核心任务）

何时用：用户要某日的候选名单、或当天生产报告口径不满足需求。

**固定参数（不得随意改）**
- 池子 = 全市场剔 ST → 换手 ≥ 1% → 成交额 ≥ 1亿
- 排序 = `weights/统一三开.json`
- 取前 30，**剔封板**（实测 Top30 里平均仅 0.3 只封板、含/剔收益相同；剔了保证名单必然可买）
- 买卖口径 = **T+1 开盘买 → T+2 收盘卖**

1. 确认 `daily/<T>/kline.jsonl` + `mf.jsonl` 存在（自动化会话 14:40 后才有）
2. 跑 `python scripts/出名单.py <T>`
3. **交付 HTML 报告** `候选_新方案_<T>.html`（脚本自动生成，表格已渲染好）；`候选_新方案_<T>.csv` 是数据件，随报告一并给出

报告自带口径说明（池子、排序权重、买卖时点），与生产报告（weights_v3 + A1open）口径不同，交付时注明用的是哪套。

---

## 任务 2：算某期效果（可选）

等 T+1、T+2 两个交易日走完，取 T+1 开盘价与 T+2 收盘价，
`收益 = T+2收盘 / T+1开盘 - 1` 逐只计算，等权平均即该期收益。
（参考：`F:/aigp/alt_screening/新方案_20260916_效果.py`）

---

## 任务 3：重新训练权重（可选，需历史数据）

```
python scripts/训练权重.py        # 需 PICKS_DATA_DIR 下有完整历史日K + 资金流 jsonl
```
产物写入本包 `weights/`。训练完必须过验证（见下），否则不得替换 `weights/统一三开.json`。

---

## 任务 4：补历史数据（可选，仅重训时需要）

```
python scripts/补数_历史K线.py
python scripts/补数_资金流.py
```

---

## 验证（重训后必须全过，缺一不可）

1. 先筛选 → 在筛后池内重跑权重
2. 双向拟合：IS(2025-04-23~) 与 OOS-B(2021-09~2024-03) 互为样本外
3. 排序层：全池日度秩 IC + 月同向率，10 口径（A=T收买→T+j开/收卖；B=T+1开买→T+j开/收卖）
4. 组合层：TopN 绝对收益，IS / OOS-A / OOS-B 三段全为正
5. 5 折时间交叉（>40/45 为正）+ bootstrap 5% 分位为正
6. 判定线：**IC > 0.01、月同向 ≥ 90%、组合三段为正、双向成立**

---

## 口径规则（执行前提，不得违反）

1. **成交额 = 量 × (1 if 688/689 else 100) × 价**；数据源直给 `amount`/`exchange` 时直接用直给值。
2. 因子一律从 K 线自算，**不使用任何含次日信息的字段**。
3. 资金流口径（腾讯 / 新浪 / 通达信）**不可混用**，选定一套用到底。
4. T 日的因子用 14:45 快照；T+1 开盘价、T+2 收盘价用官方价。
5. 因子/权重在**单票层**拟合；效果以**组合层**（TopN 等权）判断。
6. 同向性 = 排名高低 vs 区间收益的秩相关 + 名次梯度 + 头档−池均。
7. 样本纪律：单日/几十只不下结论；标准是 1000+ 天、双向样本外、月同向。
8. 成本双边 0.1~0.2%。

---

## 作业纪律

- 写新脚本前先查数据目录已有脚本，能复用就复用；先小批量试再全量跑。
- 一切实验件不进生产，结果摆出来等用户明确敲定。
- 报告不用"主口径"字样、不加星标，各口径平铺展示。
